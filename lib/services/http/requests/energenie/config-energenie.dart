class EnergenieEndpoint {
  static String root = "energenie";
  static String getConfig = "${EnergenieEndpoint.root}/config";
  static String postConfig = "${EnergenieEndpoint.root}/config/data";
  static String restartConfig = "${EnergenieEndpoint.root}/restart";
}
