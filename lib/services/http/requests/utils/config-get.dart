class GetConfig {
  static String root = "/utils";
  static String temp = "${GetConfig.root}/temp";
  static String uptime = "${GetConfig.root}/uptime";
  static String online = "${GetConfig.root}/online";
}
