class DeviceRequestURLs {
  static String root = "device-control";
  static String rebootDevice = "${DeviceRequestURLs.root}/reboot/data";
  static String shutdownDevice = "${DeviceRequestURLs.root}/shutdown/data";
}
