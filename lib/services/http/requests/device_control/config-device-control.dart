class DeviceControlEndpoint {
  static String root = "device-control";
  static String rebootDevice = "${DeviceControlEndpoint.root}/reboot/data";
  static String shutdownDevice = "${DeviceControlEndpoint.root}/shutdown/data";
}
