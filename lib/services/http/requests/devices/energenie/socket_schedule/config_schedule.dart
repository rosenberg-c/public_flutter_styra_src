class EnergenieRequestURLs {
  static String root = "energenie";
  static String getConfig = "${EnergenieRequestURLs.root}/config";
  static String postConfig = "${EnergenieRequestURLs.root}/config/data";
  static String restartConfig = "${EnergenieRequestURLs.root}/restart";
}
