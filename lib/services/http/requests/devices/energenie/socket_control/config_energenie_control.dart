class EnergenieSocketURLs {
  static String root = "/energenie";
  static String socketOff = "${EnergenieSocketURLs.root}/off/data";
  static String socketOn = "${EnergenieSocketURLs.root}/on/data";
}
